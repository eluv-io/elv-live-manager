const ButtonSpec = (defaultTextColor="#FFFFFF", defaultBackgroundColor="#000000") => ({
  text: "",
  text_color: {
    color: defaultTextColor
  },
  background_color: {
    color: defaultBackgroundColor
  }
});

export default ButtonSpec;
